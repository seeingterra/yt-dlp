#########################
# Example .env for web monitor
#########################
# Copy this file to .env (do NOT commit .env to VCS) and edit the values.
# This file demonstrates the environment variables read by devscripts/web_monitor.py

# Basic auth credentials for the web UI (optional)
YTDLP_MONITOR_USER=admin
YTDLP_MONITOR_PASS=change_me

# Optional: the default host/port are controlled via CLI args when starting the monitor.
# You can still run the monitor with --host and --port to override defaults.

# Security: keep this file private and out of source control.
